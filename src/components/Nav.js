import NavLinks from "./NavLinks"

const Nav = () => {
        return (
                <nav className="header-nav w-100 p-02 flex j-b  a-c">
                        <h1 className="logo">devMelesi</h1>

                        <NavLinks />
                </nav>
        )
}

export default Nav