const Resume = () => {
        return(
                <section className="section-resume"
                id="resume">
                        <h1 className="temp"> RESUME SECTION</h1>
                </section>
        )
}

export default Resume