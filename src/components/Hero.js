const Hero = () => {
        return (
                <section className="section-hero" id="hero">
                        <h1 className="temp">HERO SECTION</h1>
                </section>
        )
}

export default Hero