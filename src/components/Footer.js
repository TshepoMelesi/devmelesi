import FooterLinks from "./FooterLinks"

const Footer = () => {
        return (
                <footer className="main-footer flex a-c flex-d-c w-100">
                        <h2>let's link up on socials</h2>
                        <FooterLinks />
                </footer>
        )
}

export default Footer