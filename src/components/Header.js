import Nav from "./Nav"

const Header = () => {
        return (
                <header className="main-header flex">
                        <Nav />
                </header>
        )
}

export default Header